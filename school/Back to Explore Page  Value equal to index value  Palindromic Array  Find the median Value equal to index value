

class Solution {
    ArrayList<Integer> valueEqualToIndex(int arr[], int n) {
        ArrayList<Integer> brr = new ArrayList<>();
        for(int i=0;i<n;i++){
            if(arr[i] == i+1){
                brr.add(i+1);
            }
        }
        return brr;
    }
}

